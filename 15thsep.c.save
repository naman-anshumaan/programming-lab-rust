#include <stdio.h>
#include <stdlib.h>

int main(){
	FILE *infile = fopen ("blah.txt", "r");
	char s[256]; //char i will be infinitely looped since after bit 255, I++ will become 0
	for (char i = 0; i<256; i = i+1){
		S[i] = i&255;
	}

	for (i = 0; i < 256; i = i + 1){
		printf("%d -> %d", i, S[i]);
	}
	
	return 0;

}
